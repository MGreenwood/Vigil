# Database Configuration
DATABASE_URL=postgresql://vigil:vigil@localhost:5432/vigil?sslmode=disable

# Redis Configuration
REDIS_URL=redis://localhost:6379

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
ENVIRONMENT=development

# Server Configuration
PORT=8080

# Cloudflare Tunnel (optional)
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token

# Email Configuration (for notifications)
# Development: Use Mailhog (no auth needed)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=noreply@vigil.rest

# Production: Use your @vigil.rest email service
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=noreply@vigil.rest
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@vigil.rest

# Slack Configuration (for notifications)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Discord Configuration (for notifications)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR/DISCORD/WEBHOOK 